# O build de produção (Render) roda com NODE_ENV=production, o que faz o npm omitir
# devDependencies por padrão. Como o build precisa de `typescript` e dos `@types/*`,
# e o seed roda via `tsx`, forçamos a instalação delas.
include=dev
